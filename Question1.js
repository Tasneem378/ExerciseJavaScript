//    1-QUESTION.Explain the difference between forEach, map, filter, and reduce.

//         forEach(), is used to execute the same code on every element in an
//         array but does not change the array. map() executes the same code on
//         every element in an array and returns a new array with the updated
//         elements.  filter() checks every element in an array to see if it meets
//         a certain criteria and returns a new array with the elements that return
//         truthy for the criteria. reduce() Reduce takes a callback function. The
//         call back function takes accumulator, current, and optional initial
//         value as a parameter and returns a single value.
