// 3-QUESTION.Use forEach to console.log each country in the countries array.

const countries = [
    "Finland",
    "Sweden",
    "England",
    "Norway",
    "Iceland",
    "Ireland",
  ];
  countries.forEach((country) => {
      console.log(country);
  });
