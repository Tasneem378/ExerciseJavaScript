// 9-QUESTION.Use map to change to each name to uppercase in the names array

const names = ["Asabeneh", "Mathias", "Elias", "Brook"];
const namesUpperCase = names.map((name) => name.toUpperCase());
console.log(namesUpperCase);